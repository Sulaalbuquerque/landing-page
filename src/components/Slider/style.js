import styled from 'styled-components'

export const ContainerSlider = styled.div`
    .swiper-slide{
        display: flex;
        justify-content: center;
    }

    .swiper-button-prev,
    .swiper-button-next{
        color: black;
    }
`